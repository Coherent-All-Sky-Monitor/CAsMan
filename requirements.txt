# CAsMan Package

# Flask web framework for web interfaces
flask>=2.0.0

# Flask-CORS for mobile browser access
flask-cors>=3.0.0

# Gunicorn for production WSGI server (optional)
gunicorn>=21.0.0

# NumPy for kernel index array operations
numpy>=1.20.0

# boto3 for cloud storage (R2/S3) backup and sync (deprecated, kept for compatibility)
boto3>=1.26.0

# requests for GitHub API communication (database sync)
requests>=2.28.0
